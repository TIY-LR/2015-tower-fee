import { padZero } from '../../../helpers/pad-zero';
import { module, test } from 'qunit';

module('Unit | Helper | pad zero');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = padZero(42);
  assert.ok(result);
});
